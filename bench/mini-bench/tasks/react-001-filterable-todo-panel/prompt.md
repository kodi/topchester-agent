You are in a small React + TypeScript project. Implement the filterable todo panel behavior described in README.md.

The expected validation command is:

```sh
pnpm test
```

Keep the implementation general. Do not hard-code the sample todos.
