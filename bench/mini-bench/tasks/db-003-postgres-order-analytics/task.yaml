id: db-003-postgres-order-analytics
name: Postgres Order Analytics
category: database
difficulty: medium
prompt: prompt.md
workspace: workspace
bootstrap:
  script: pnpm install --ignore-workspace --frozen-lockfile
services:
  - postgres
verifier:
  command: pnpm exec tsx verifier/verify.ts
timeoutMs: 300000
agent:
  cwd: /workspace
  profile: mini-bench
expected:
  changedFiles:
    - src/analytics.ts
