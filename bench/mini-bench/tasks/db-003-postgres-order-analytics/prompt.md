You are in a small TypeScript project backed by PostgreSQL through the `pg` package. Implement the analytics behavior described in README.md.

The expected validation command is:

```sh
pnpm test
```

Keep the implementation general. Do not hard-code the sample customers, orders, dates, or regions.
