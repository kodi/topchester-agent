You are in a small TypeScript project. Implement the legacy config migration behavior described in README.md.

The expected validation command is:

```sh
pnpm test
```

Keep the implementation general. Do not hard-code the sample records.
