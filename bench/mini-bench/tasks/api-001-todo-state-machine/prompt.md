You are in a small TypeScript Express project. Implement the todo state-machine API described in README.md.

The expected validation command is:

```sh
pnpm test
```

Keep the implementation general. Do not hard-code the sample todos, ids, or timestamps.
