You are in a small TypeScript project using Node's built-in SQLite support. Implement the ledger behavior described in README.md.

The expected validation command is:

```sh
pnpm test
```

Keep the implementation general. Do not hard-code the sample accounts or transactions.
