{
  "id": "11-write-file",
  "name": "Write file",
  "description": "Checks write_file can create a new file under a new parent directory.",
  "timeoutMs": 10000,
  "prompt": "Create test/generated-smoke.test.ts with a simple Vitest test named WRITE_FILE_SMOKE that includes the exact assertion expect(true).toBe(true).",
  "requiredToolCalls": ["write_file"],
  "forbiddenToolCalls": ["edit_file", "inspect_command"],
  "expectedFiles": ["test/generated-smoke.test.ts"],
  "expectedContent": [
    { "file": "test/generated-smoke.test.ts", "contains": "WRITE_FILE_SMOKE" },
    { "file": "test/generated-smoke.test.ts", "contains": "expect(true).toBe(true)" }
  ],
  "expectedKb": {
    "statusAfter": "needs_sync"
  }
}
